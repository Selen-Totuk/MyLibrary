services:
  - type: web
    name: mylibrary-web
    env: dotnet
    plan: free
    repo: https://github.com/your-username/your-repo
    branch: main
    buildCommand: 'dotnet publish -c Release -o publish'
    startCommand: 'dotnet MyLibrary.dll'
    # Set environment variables on Render dashboard: CONNECTION_STRING (Postgres URI) or leave for SQLite demo

staticSites: []
