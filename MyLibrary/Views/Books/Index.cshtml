@model IEnumerable<MyLibrary.Models.Book>
@{ ViewData["Title"] = "Kitaplarım"; }

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="fw-bold">📚 Kitap Listesi</h2>
    <a asp-action="Create" class="btn btn-primary px-4">+ Yeni Ekle</a>
</div>

<div class="book-grid">
    @foreach (var book in Model)
    {
        <div class="book-card status-@book.Status">
            <img src="@(book.ThumbnailPath ?? book.CoverImage ?? "/images/default.png")" alt="@book.Title">
            <div class="p-3">
                <h6 class="fw-bold mb-1 text-truncate">@book.Title</h6>
                <p class="small text-secondary mb-3">@book.Author</p>
                <div class="d-flex justify-content-between">
                    <a asp-action="Delete" asp-route-id="@book.Id" class="btn btn-sm btn-outline-danger" 
                       onclick="return confirm('Silmek istediğine emin misin?')">🗑️</a>
                </div>
            </div>
        </div>
    }
</div>