@{ 
    ViewData["Title"] = "Giriş Yap"; 
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-5 col-lg-4">
            <div class="card shadow-lg border-0 p-4" style="border-radius:24px; background: var(--card-bg);">
                <div class="text-center mb-4">
                    <div style="font-size: 64px; margin-bottom: 16px;">📚</div>
                    <h3 class="fw-bold" style="color: var(--text-main);">Kütüphaneme Giriş</h3>
                    <p class="text-muted small">Kitaplarınıza erişmek için giriş yapın</p>
                </div>
                
                @if (ViewBag.Error != null) 
                { 
                    <div class="alert alert-danger d-flex align-items-center" style="border-radius:12px; border:none; background: #fee2e2; color: #991b1b;">
                        <span style="font-size:20px; margin-right:8px;">⚠️</span>
                        <span>@ViewBag.Error</span>
                    </div> 
                }
                
                <form asp-action="Login" method="post">
                    @Html.AntiForgeryToken()
                    
                    <div class="mb-3">
                        <label class="form-label fw-semibold" style="color: var(--text-main);">E-posta Adresi</label>
                        <input name="email" 
                               type="email" 
                               class="form-control form-control-lg" 
                               placeholder="ornek@email.com"
                               required 
                               style="border-radius:12px; background: var(--bg-main); color: var(--text-main); border: 2px solid var(--border-color);" />
                    </div>
                    
                    <div class="mb-4">
                        <label class="form-label fw-semibold" style="color: var(--text-main);">Şifre</label>
                        <input name="password" 
                               type="password" 
                               class="form-control form-control-lg" 
                               placeholder="••••••••"
                               required 
                               style="border-radius:12px; background: var(--bg-main); color: var(--text-main); border: 2px solid var(--border-color);" />
                    </div>
                    
                    <button type="submit" 
                            class="btn btn-lg w-100 mb-3 shadow" 
                            style="border-radius:12px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border:none; color: white; font-weight: 600;">
                        🔐 Giriş Yap
                    </button>
                    
                    <div class="text-center">
                        <p class="text-muted small mb-2">Hesabınız yok mu?</p>
                        <a asp-action="Register" 
                           class="btn btn-outline-primary w-100" 
                           style="border-radius:12px; border: 2px solid var(--accent); color: var(--accent); font-weight: 600;">
                            ✨ Hemen Kaydol
                        </a>
                    </div>
                </form>
            </div>
            
            <div class="text-center mt-3">
                <a asp-controller="Books" asp-action="Index" class="text-muted small text-decoration-none">
                    ← Ana sayfaya dön
                </a>
            </div>
        </div>
    </div>
</div>

<style>
.form-control:focus {
    border-color: var(--accent) !important;
    box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25) !important;
    background: var(--bg-main) !important;
    color: var(--text-main) !important;
}

body.theme-dark .form-control {
    background: var(--card-bg) !important;
    color: var(--text-main) !important;
    border-color: var(--border-color) !important;
}

body.theme-dark .alert-danger {
    background: #3f1f1f !important;
    color: #fca5a5 !important;
}

.btn-outline-primary:hover {
    background: var(--accent) !important;
    color: white !important;
    transform: translateY(-2px);
    transition: all 0.3s;
}
</style>